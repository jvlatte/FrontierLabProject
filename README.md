Command Line Prompt Format:


--backend; "cpu", "gpu", "compare"
--tasks; "statevector", "sampling"
--circuit; "random", "ghz", "qft"
--nqubits; int
--depth; int
--shots; int (only for --tasks sampling)
--repeats; int (number of times to execute program)
--seed; random seed int