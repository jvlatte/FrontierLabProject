[build-system]
requires = ["scikit-build-core>=0.9", "pybind11>=2.11"]
build-backend = "scikit_build_core.build"

[project]
name = "qgpusim-cuda"
version = "0.0.1"
description = "CUDA extension for qgpusim"
requires-python = ">=3.9"

[tool.scikit-build]
build-dir = "build"
wheel.packages = []